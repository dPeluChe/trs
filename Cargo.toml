[package]
name = "tars-cli"
version = "0.1.0"
edition = "2024"
description = "TARS CLI tool"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
